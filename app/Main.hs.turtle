-- cp.hs

{-# LANGUAGE OverloadedStrings #-}

import Turtle
import Prelude hiding (FilePath)

parser :: Parser (FilePath, FilePath)
parser = (,) <$> argPath "src"  "The sourse file"
             <*> argPath "dest" "The destination file"

main = do
    --dir <- pwd
    --print dir
    (src, dest) <- options "A simple `cp` utility" parser
    cp src dest

